version: "3.5"
services:
  headscale:
    image: headscale/headscale:latest
    volumes:
      - ./config:/etc/headscale/
    ports:
      - 8080:8080
      - 443:443
    command: headscale serve
    restart: unless-stopped
